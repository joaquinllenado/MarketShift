┌──────────┬──────────────────────────────────────────────────┐
│ Terminal │ Command                                          │ 
├──────────┼──────────────────────────────────────────────────┤
│ Backend  │ `cd backend && source venv/bin/activate &&       │ 
│          │ uvicorn main:app                                 │ 
│          │ --reload`                                        │ 
│ Frontend │ cd frontend && npm run dev                       │ 
└──────────┴──────────────────────────────────────────────────┘

- 
curl http://localhost:8000/hello → {"message":"Hello World from Prefect!"}
- 
curl http://localhost:5173/api/hello → {"message":"Hello World from Prefect!"} (proxy works)

EXA
- Company search
- Find competitors
    - Scrape home, pricing, blog/changelog, integrations
    
product hunt, news, user reviews

bucket changes into signals like pricing, positioning, features, CTAs, and content narrative, then outputs “what changed” + “what it means” + 2–3 suggested product moves. Scope for hackathon: one input screen (paste 3–5 URLs), one click to fetch, one dashboard‑style summary, optional “read my brief” voice button.

document database, store metrics, find updates and compare 

at the end, generate next steps

grab logo, link to source
partnerships ("OpenAI announced partnership with Meta"), link to source
market & icp signals


market opportunities (for the user's company)
market gaps